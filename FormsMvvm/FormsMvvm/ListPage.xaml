<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FormsMvvm.ListPage"
             x:Name="contentPage">
    <ContentPage.Content>
        <Grid BindingContext="{x:Reference contentPage}">
            <StackLayout>
                <Label Text="{Binding ErrorMessage}" IsVisible="{Binding IsError}" TextColor="Red" FontSize="Micro"/>
                <StackLayout>
                    <Label Text="{Binding Path=ResultsAvailable, StringFormat='Available {0}'}"/>
                    <Label Text="{Binding Path=ResultsReturned, StringFormat='Returned {0}'}"/>
                    <Label Text="{Binding Path=ResultsStart, StringFormat='Start {0}'}"/>
                </StackLayout>
                <ListView ItemsSource="{Binding Items}" Footer="FOOT" Header="HEAD">
                    <ListView.HeaderTemplate>
                        <DataTemplate>
                            <Label Text="{Binding}" BackgroundColor="AliceBlue"/>
                        </DataTemplate>
                    </ListView.HeaderTemplate>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Image Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Source="{Binding Path=logo_image}"/>
                                    <Label Grid.Row="0" Grid.Column="1" Text="{Binding Path=name}" FontSize="Default"/>
                                    <Label Grid.Row="1" Grid.Column="1" Text="{Binding Path=station_name}" FontSize="Micro"/>
                                </Grid>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.FooterTemplate>
                        <DataTemplate>
                            <Button Text="{Binding}" BackgroundColor="AliceBlue"/>
                        </DataTemplate>
                    </ListView.FooterTemplate>
                </ListView>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>